# Frontend - Web Chat

# Будь ласка прочитайте !

Простий та елегантний веб-чат з Notion-стилем дизайну.

## 🚀 Технології

- **React 19** + **Vite**
- **GSAP** - для анімацій
- **Canvas Confetti** - для святкових ефектів
- **CSS3** - чистий CSS без фреймворків

## 📁 Структура проєкту

```
frontend/src/
├── components/          # Всі компоненти тут
│   └── Welcome/         # Кожен компонент у своїй папці
│       ├── Welcome.jsx  # Логіка компонента
│       └── Welcome.css  # Стилі компонента
├── assets/              # Картинки, іконки
├── App.jsx              # Головний компонент
├── App.css              # Стилі для App
├── main.jsx             # Точка входу
└── index.css            # Глобальні стилі
```

## 🎨 Дизайн система (Notion-style)

**Кольори:**
- Фон: `#ffffff` (білий)
- Текст: `#37352f` (темно-сірий)
- Шрифт: `-apple-system, BlinkMacSystemFont, 'Segoe UI'`

**Принципи:**
- ✅ Мінімалізм - нічого зайвого
- ✅ Швидкі анімації (0.4-0.6s)
- ✅ Snappy відчуття (як у Notion)

## 📝 Правила для команди

### 1. Структура компонентів

**Кожен компонент у своїй папці:**
```
components/
└── ComponentName/
    ├── ComponentName.jsx
    └── ComponentName.css
```
**Приклад:**
```jsx
// components/Button/Button.jsx
import './Button.css'

const Button = ({ children, onClick }) => {
  return (
    <button className="button" onClick={onClick}>
      {children}
    </button>
  )
}

export default Button
```

### 2. Іменування

- **Компоненти**: `PascalCase` (Button.jsx, ChatWindow.jsx)
- **CSS класи**: `kebab-case` (.button, .chat-window)
- **Функції**: `camelCase` (handleClick, getUserData)

### 3. CSS стилі

**Використовуй прості класи:**
```css
.component-name {
  /* Стилі */
}
```

**НЕ використовуй:**
- ❌ Inline стилі (style={{...}})
- ❌ CSS-in-JS бібліотеки
- ❌ CSS модулі

### 4. Анімації (GSAP)

**Приклад простої анімації:** # Вам не потібно розбиратися з цим, моя мета не робити 200 анімацій, а зробити сайт який буде відчуватися швидким і на вигляд приємним, якщо маєте ідеї де добавити анімації, я завжди готовий вислухати
```jsx
import { useEffect, useRef } from 'react'
import gsap from 'gsap'

const Component = () => {
  const ref = useRef(null)

  useEffect(() => {
    gsap.from(ref.current, {
      opacity: 0,
      y: 20,
      duration: 0.5
    })
  }, [])

  return <div ref={ref}>Контент</div>
}
```

### 5. Git workflow

**Перед роботою:**
```bash
git checkout dev
git pull origin dev
git checkout -b frontend-й ім'я твоєї фічі наприкоад frontend-navbar
```

**Після роботи:**
```bash
git add .
git commit -m "frontend: опис змін"
git push origin frontend-твоя-фіча
```

**Створи Pull Request у гілку `dev`**

### 6. Commit messages

**Формат:** `frontend: що зробив`

**Приклади:**
```
frontend: додав компонент Welcome
frontend: налаштував GSAP анімації
frontend: виправив баг з тремтінням тексту
frontend: додав responsive дизайн
```

## ✅ Що вже готово

- ✅ Vite + React налаштовано
- ✅ GSAP підключено
- ✅ Структура проєкту
- ✅ Welcome екран з анімацією
- ✅ Notion-style дизайн

## 📋 Що далі
Я пізніше опишу в trello 
- ⏳ Login/Register компоненти
- ⏳ Chat компонент
- ⏳ Список контактів
- ⏳ Відправка повідомлень

## 💡 Поради

1. **Перевіряй код перед commit:**
   ```bash
   npm run lint
   npm run dev  # переконайся що працює
   ```

2. **Пиши коментарі коли потрібно:**
   ```jsx
   // Анімація появи тексту
   useEffect(() => { ... })
   ```


## 🆘 Проблеми?

1. Перевір чи встановлені залежності: `npm install`
2. Видали node_modules і встанови знову
3. Перевір чи правильний шлях до файлу
4. Подивись консоль браузера (F12)
---